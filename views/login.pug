extends layouts/main

block content
    .form__container
        form(action=`${routes.login}`, method="post")
            input(type="email", name="email", placeholder="Email" ,required=tru)
            input(type="password", name="password", placeholder="Password" ,required=tru)
            input(type="submit",value="Log In")
        include partials/socialLogin