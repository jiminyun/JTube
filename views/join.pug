extends layouts/main

block content
    .form-container 
        form(action=routes.join, method="post")
            input(type="text", name="name", placeholder="Full Name",required=tru)        
            input(type="email", name="email", placeholder="Email",required=tru)
            input(type="password", name="password", placeholder="Password",required=tru)
            input(type="password", name="password2", placeholder="Verify Password",required=tru)
            input(type="submit",value="Join Now")
            input(type="text", name="name" placeholder="Full Name")
        include partials/socialLogin    